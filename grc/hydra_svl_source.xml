<?xml version="1.0"?>
<block>
  <name>hydra_source</name>
  <key>hydra_hydra_source</key>
  <category>hydra</category>
  <import>import hydra</import>
  <make>hydra.hydra_source($num_outputs, $fft_m_size, $fft_n_size)</make>


  <param>
    <name>Num VRadios</name>
    <key>num_outputs</key>
    <value>2</value>
    <type>int</type>
  </param>
  <param>
    <name>FFT M Size</name>
    <key>fft_m_size</key>
    <value>1024</value>
    <type>int</type>
  </param>
  <param>
    <name>FFT N Size</name>
    <key>fft_n_size</key>
    <value>()</value>
    <type>raw</type>
  </param>

  <check>$num_outputs &gt; 0</check>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>complex</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type>complex</type>
    <vlen>$fft_m_size</vlen>
  </source>

</block>
